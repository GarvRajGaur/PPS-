#include <stdio.h>
int main(){
    float r , a;
    printf("enter radius for area:");
    scanf("%f",&r);

    a=3.14*r*r;
    printf("area of circle with radius %f is %f", r , a);
        return 0;
}
